// Pin.js

import React from 'react';
import '../styles/Pin.css'; // Import your CSS file for styling

const Pin = () => {
  return (
    <div className="pin-container">
      <div className="pin" />
    </div>
  );
};

export default Pin;
